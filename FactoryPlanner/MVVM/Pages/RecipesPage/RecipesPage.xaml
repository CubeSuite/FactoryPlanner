<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="FactoryPlanner.MVVM.Pages.RecipesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:FactoryPlanner.MVVM.Pages"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:viewModels="using:FactoryPlanner.Core.MVVM.Models.ViewModels"
      xmlns:views="using:FactoryPlanner.MVVM.Views"
      Margin="8"
      mc:Ignorable="d">

    <Grid ColumnSpacing="8"
          RowSpacing="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox Text="{Binding SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 PlaceholderText="Search..." />

        <Button Content="Create New Recipe"
                Style="{StaticResource AccentButtonStyle}"
                Grid.Column="1"
                Command="{Binding CreateNewRecipeCommand}" />

        <ListView ItemsSource="{Binding FilteredRecipes}"
                  Grid.ColumnSpan="2"
                  Grid.Row="1"
                  Margin="-16,0"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:RecipeViewModel">
                    <views:RecipeView Margin="0,0,0,8" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
