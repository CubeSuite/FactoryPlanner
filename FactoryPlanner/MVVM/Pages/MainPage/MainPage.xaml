<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="FactoryPlanner.MVVM.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:FactoryPlanner.MVVM.Pages"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid>
        <NavigationView x:Name="navMenu"
                        IsBackButtonVisible="Collapsed"
                        IsEnabled="{Binding NavMenuEnabled}"
                        IsPaneOpen="False"
                        IsPaneToggleButtonVisible="True"
                        ItemInvoked="OnNavigationViewItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Items">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xF8B0;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Machines">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC5B;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Recipes">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE9A1;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Boosts">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE945;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Factory Planner">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC07;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Backups"
                                    Icon="Save"
                                    Visibility="{Binding BackupsButtonVisibility}" />
            </NavigationView.FooterMenuItems>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <ItemsRepeater ItemsSource="{Binding Notifications}">
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <InfoBar Title="{Binding Title}"
                                     IsOpen="True"
                                     Message="{Binding Message}"
                                     Severity="{Binding Severity}">
                                <InfoBar.ActionButton>
                                    <Button Content="{Binding ButtonText}"
                                            Command="{Binding ActionButtonClickedCommand}"
                                            Visibility="{Binding ActionButtonVisibility}" />
                                </InfoBar.ActionButton>
                            </InfoBar>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
                <Frame x:Name="pageFrame"
                       Grid.Row="1" />

            </Grid>
        </NavigationView>

    </Grid>
</Page>
